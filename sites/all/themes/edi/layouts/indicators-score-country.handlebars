<div class="indicators">
  {{#if indicatorsLaw}}
    <div class="indicator-title">
      <span>LAW</span>
      <a href="#" data-type="Law" class="more-info-type-indicator notranslate">?</a>
    </div>
    <ul class="indicators-index">
      {{#each indicatorsLaw}}
        <li class="indicator" data-number="{{indicatorNumber}}" data-type="{{type}}">
          <p><span>Indicator {{this.indicatorNumber}}:</span>
          {{this.indicatorText}}
          </p>
          <div class="more-info-and-rrss">
            <a class="more-info more-info-indicator" href="#">?</a>
            <a class="more-info more-info-btn notranslate" href="#" data="{{this.indicatorNumber}}" data-type="{{type}}">i</a>
            {{!-- <a href="#" class="rrss"></a> --}}
          </div>
          <span class="score score-{{this.score}}">{{this.score}}</span>
        </li>
      {{/each}}
    </ul>
  {{/if}}
  {{#if indicatorsPractice}}
    <div class="indicator-title">
      <span>PRACTICE</span>
      <a href="#" data-type="Practice" class="more-info-type-indicator notranslate">?</a>
    </div>
    <ul class="indicators-index">
      {{#each indicatorsPractice}}
        <li class="indicator" data-number="{{indicatorNumber}}" data-type="{{type}}">
          <p><span>Indicator {{this.indicatorNumber}}:</span>
          {{this.indicatorText}}
          </p>
          <div class="more-info-and-rrss">
            <a class="more-info more-info-indicator" href="#">?</a>
            <a class="more-info more-info-btn notranslate" href="#" data="{{this.indicatorNumber}}" data-type="{{type}}">i</a>
            {{!-- <a href="#" class="rrss"></a> --}}
          </div>
          <span class="score in-practice score-{{this.score}}">{{this.score}}</span>
        </li>
      {{/each}}
    </ul>
  {{/if}}
</div>
